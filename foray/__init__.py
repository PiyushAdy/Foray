"""Foray: the local intelligence layer for your codebase.

A local-first developer tool that indexes a repository to provide grounded
context for humans (via the web UI) and agents (via the MCP server).
"""

__version__ = "0.1.0"
